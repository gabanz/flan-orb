description: >
  Start scanning.

steps:
  - run:
      name: Start scanning
      command: |
        pwd
        ls
        docker run --name flan_scan \
        -v $(CURDIR)/shared:/shared \
        -e format=html \
        flan_scan
        docker cp /shared/ips.txt flan_scan:/shared

